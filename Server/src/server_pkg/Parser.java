package server_pkg;

import java.util.Scanner;

public class Parser {

	public void Parse(String str) {
		Scanner s = new Scanner(str);
		s.useDelimiter("#");
		if(s.next() == "L") { //Leave Lobby
			
			
		}
	}
}
